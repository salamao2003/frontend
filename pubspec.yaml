name: egy_metro
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  flutter_bloc: ^8.1.6
  http: ^1.2.2
  url_launcher: ^6.2.5  # تحديث الإصدار
  geolocator: ^10.1.0   # تحديث الإصدار لحل مشاكل التوافق

  # إضافات مقترحة لتحسين التطبيق
  permission_handler: ^11.3.0  # للتعامل مع الأذونات بشكل أفضل
  google_maps_flutter: ^2.5.3  # إذا كنت تخطط لإضافة خرائط

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1  # تحديث الإصدار

flutter:
  uses-material-design: true

  assets:
    - assets/Cairo_metro_logo.png
    - assets/Cairo_Metro_Map.png
    - assets/wallet.png
    - assets/visa.png
    - assets/vodafone.png